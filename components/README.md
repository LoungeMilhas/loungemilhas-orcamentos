# components/

Componentes de UI reutilizaveis. Estrutura prevista para a Fase 1:

- componentes de marca (header/footer/CTA com logo e paleta da Lounge Milhas);
- blocos do orcamento por tipo de item: `ItemVoo`, `ItemHotel`, `ItemPasseio`, `ItemTransfer`, `ItemServico`;
- `OrcamentoRenderer` (orientado a template + identidade visual automatica);
- componentes do painel admin (formularios, builder de itens, uploader).

Vazio nesta etapa (Etapa 0). Implementacao na Etapa 1.
